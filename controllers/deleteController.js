const express = require('express')
const server = express();
const userData = require("../models/dataStorage");
const fs = require("fs");
const path = require("path");

const deleteController = (req, res) => {
}

module.exports = deleteController;